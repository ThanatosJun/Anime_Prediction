# Ablation Branch

Project-internal ablation experiments for RQ analysis.

Run:

```bash
python -m src.ablation_branch.run_ablation_baselines
```

Config:

- `configs/ablation_baselines.yaml`

Planned groups:

- `A1-Modality`
- `A2-ImageSource`
- `A3-TextSource`
- `A4-RAG`
- `A5-Fusion`

