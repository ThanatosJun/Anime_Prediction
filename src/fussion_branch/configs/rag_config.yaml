qdrant:
  collection_name: "anime_rag"
  db_path: "qdrant_db"

paths:
  encoder_path: "src/fussion_branch/RAG/sparse_encoder.json"
  train_csv: "data/fussion/fusion_meta_clean_train.csv"
  text_emb_dir: "src/fussion_branch/RAG"
  out_dir: "src/fussion_branch/RAG/returen"

embedding:
  text_emb_dim: 384

indexing:
  batch_size: 256

query:
  top_k: 10
  prefetch_k: 20
