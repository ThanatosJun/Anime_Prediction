# Experiment Common

Shared utilities for experiment code.

This package is intentionally not a baseline group. It only contains reusable
pieces such as train-only feature encoders and metrics so reference baselines
and ablation experiments stay comparable.

