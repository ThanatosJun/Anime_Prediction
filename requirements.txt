# ── Deep Learning ─────────────────────────────────────────────────────────────
# PyTorch: install separately with CUDA 12.8 support (RTX 5070 Ti / Blackwell)
# pip install torch torchvision --index-url https://download.pytorch.org/whl/cu128
torch==2.11.0
torchvision==0.26.0
tensorboard==2.20.0

# ── Hugging Face ──────────────────────────────────────────────────────────────
transformers==5.6.2
sentence-transformers==5.4.1
huggingface_hub==1.12.0
tokenizers==0.22.2
safetensors==0.7.0

# ── Data / ML ─────────────────────────────────────────────────────────────────
pandas==3.0.2
numpy>=2.0
scikit-learn==1.8.0
scipy==1.17.1
pyarrow==24.0.0

# ── Image ─────────────────────────────────────────────────────────────────────
Pillow==12.2.0
# dghs-imgutils metadata 宣告 numpy<2，與 shap>=0.51 衝突，但執行時相容
# 需單獨安裝：pip install dghs-imgutils==0.19.0 --no-deps
# dghs-imgutils==0.19.0

# ── YOLO / ONNX Runtime ───────────────────────────────────────────────────────
# onnxruntime-gpu 1.x 依賴 CUDA 12 函式庫；CUDA 13 環境需手動補裝：
onnxruntime-gpu==1.26.0
nvidia-cublas-cu12==12.9.2.10
nvidia-cuda-runtime-cu12==12.9.79
nvidia-cufft-cu12==11.4.1.4
nvidia-cuda-nvrtc-cu12==12.9.86
nvidia-curand-cu12==10.3.10.19
nvidia-cusparse-cu12==12.5.10.65
nvidia-cusolver-cu12==11.7.5.82
nvidia-nvjitlink-cu12==12.9.86
# 啟動時須設定 LD_LIBRARY_PATH，conda activate.d/ort_cuda12_libs.sh 已自動處理

# ── Vector Database ───────────────────────────────────────────────────────────
qdrant-client==1.17.1

# ── Utilities ─────────────────────────────────────────────────────────────────
requests==2.33.1
tqdm==4.67.3
PyYAML==6.0.3

# ── Visualisation ─────────────────────────────────────────────────────────────
matplotlib==3.10.9
seaborn==0.13.2          # 統計視覺化（heatmap、distribution plot 等），用於 EDA notebook

# ── Explainability ────────────────────────────────────────────────────────────
shap==0.51.0             # SHAP feature importance (GradientExplainer for PyTorch)

# ── Notebook ──────────────────────────────────────────────────────────────────
ipykernel==7.2.0
