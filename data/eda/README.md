# EDA Outputs

This directory stores lightweight, reproducible EDA outputs.

- Keep concise summaries (`*_summary.md`, `*_summary.json`) under version control.
- Keep heavy artifacts (full plots, temporary extracts) out of Git.
